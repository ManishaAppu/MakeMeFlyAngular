import { SearchPnrPipe } from './search-pnr.pipe';

describe('SearchPnrPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPnrPipe();
    expect(pipe).toBeTruthy();
  });
});
